import { createStore } from 'vuex'
import field from '@/store/modules/field.js';
import students from '@/store/modules/students.js';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    field,
    students
  }
})